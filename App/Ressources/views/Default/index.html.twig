<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
</head>
<body>
    <ul>
        {% for p in Products %}
            <li>{{ p.name }}</li>
        {% endfor %}
    </ul>
    {{ path('homepage') }}
</body>
</html>