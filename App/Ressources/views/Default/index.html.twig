<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Title</title>
</head>
<body>
    <b>Bonjour {{ name|capitalize }}</b>

    <ul>
        {% for p in Products %}
            <li>{{ p.name }}</li>
        {% endfor %}
    </ul>
</body>
</html>